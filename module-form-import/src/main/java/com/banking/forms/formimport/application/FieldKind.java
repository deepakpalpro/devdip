package com.banking.forms.formimport.application;

/** Neutral field kinds produced by extraction, before mapping onto the renderer's schema types. */
public enum FieldKind {
    TEXT,
    NUMBER,
    CHOICE,
    CHECKBOX
}
